#include <stdio.h>

int main() {

// Ternary Operator --> is the shortcut to if-else statements
// (condition) ? execute if condition is true : execute if condition is false

int x = 3, y = 2;

x < y ? printf("x is less than y \n") : printf("x is not less than y \n");

    return 0;
}